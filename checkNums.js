num1 = 2
num2 = 3;
if (num1 < num2) {
	return "true"
} else if (num2 < num1) {
	return "false"
} else if (num1 === num2) {
	return "-1"
}

// ------------------------- //
if (num2 > num1) {
	return true
} else if (num1 > num2){
	return false;
} else if (num1 === num2) {
	return "-1"
}

// =====================================================================================================================
																// FUNCTIONAL - INCOMPLETE
// =====================================================================================================================
// passed 2 numbers
// put them into array
// compare/REDUCE?

function addToArr(num1, num2) {
	return arr[num1, num2]
}

function compare(arr) {
	return arr.sort(function(a,b) {
		return a - b
	})
}

function returnCompar(arr) {
	if arr[0] 
}
